services:
  plemperer:
    build: .
    ports:
      - "9999:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data # Optional: customize data directory path
    restart: unless-stopped
